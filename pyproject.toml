[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "scripting"
version = "0.1.0"
description = "Scripting project modules."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
py-modules = ["scripting", "robot", "engine"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cmdtools"
version = "0.1.0"
description = "Command-dispatch tools."
requires-python = ">=3.8"
authors = [
  {name = "John Lin"},
  {name = "GPT-5.2-Codex"},
]
dependencies = []

[tool.setuptools]
py-modules = ["cmdtools"]
